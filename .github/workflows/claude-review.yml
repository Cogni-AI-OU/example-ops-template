---
name: PR Review

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  actions: read
  contents: read
  issues: read
  pull-requests: write
  id-token: write

jobs:
  claude-review:
    uses: Cogni-AI-OU/.github/.github/workflows/claude-review.yml@dev
    secrets: inherit
